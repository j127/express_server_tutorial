<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>{{title}}</title>
        <link rel="stylesheet" href="/styles.css">
    </head>
    <body>
        <nav>
            <a href="/">Home</a> | <a href="/about">About</a>
        </nav>
        <h1>{{title}}</h1>
        <p>{{body}}</p>
        {{#each drinks}}
            {{increment @index}}. {{.}}
        {{/each}}
        <script src="/main.js"></script>
    </body>
</html>
